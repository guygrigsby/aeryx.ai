{
  "name": "aeryx-site",
  "type": "module",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "brand": "for f in tokens.css brand.css fonts.css; do curl -fsSL --create-dirs -o src/styles/vendor/$f https://raw.githubusercontent.com/guygrigsby/aeryx-brand/$npm_package_config_brandRef/brand/$f || exit 1; done && for f in hack-regular.woff2 hack-bold.woff2 LICENSE.md; do curl -fsSL --create-dirs -o public/fonts/$f https://raw.githubusercontent.com/guygrigsby/aeryx-brand/$npm_package_config_brandRef/brand/fonts/$f || exit 1; done"
  },
  "config": {
    "brandRef": "1bc7d16cd49b25b97553a7d31a20aaae76a2324b"
  },
  "devDependencies": {
    "astro": "^5.0.0",
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "jsdom": "^25.0.0",
    "wrangler": "^4.0.0"
  }
}
